import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
    },
    backgound: {
        height: "100%",
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        alignContent: 'center',

    },
    button: {
        color: "#00000070"
    }
});